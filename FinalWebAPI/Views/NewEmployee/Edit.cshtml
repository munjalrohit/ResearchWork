@model FinalWebAPI.Models.NewEmployee

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>NewEmployee</legend>

        @Html.HiddenFor(model => model.ID)

        <div class="editor-label">
            @Html.LabelFor(model => model.EmpName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.EmpName)
            @Html.ValidationMessageFor(model => model.EmpName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.EmpDepartment)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.EmpDepartment)
            @Html.ValidationMessageFor(model => model.EmpDepartment)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.EmpMobile)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.EmpMobile)
            @Html.ValidationMessageFor(model => model.EmpMobile)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
